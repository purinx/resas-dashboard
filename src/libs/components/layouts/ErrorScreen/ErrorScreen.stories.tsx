import { Meta, StoryObj } from '@storybook/react';

import { ErrorScreen } from '.';

const meta: Meta<typeof ErrorScreen> = {
  component: ErrorScreen,
};

export default meta;

export const ErrorScreenStory: StoryObj = {
  args: {},
};
